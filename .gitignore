# Portable build tooling (Maven / PostgreSQL) downloaded for local verification
tools/

# OS / IDE
.DS_Store
.idea/
.vscode/

# Runtime logs (backend writes here per application.yml's logging.file.name)
logs/
